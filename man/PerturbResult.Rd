% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EffectMat_func.R
\name{PerturbResult}
\alias{PerturbResult}
\title{Calculate graph coefficients after gene perturbation}
\usage{
PerturbResult(
  net_struc,
  data,
  meta = NULL,
  index = NULL,
  n_sample = 1,
  n_permutation = 1,
  perturb_ratio = 0,
  mode = "single_cell",
  perturbation = "single_feature",
  ncores = 1,
  verbal = FALSE,
  replace = FALSE,
  oracle = NULL,
  links = NULL,
  save_dir = NULL,
  group.by = "celltype",
  assay = "RNA",
  layer = "data",
  genes = NULL,
  perturb_value = 0,
  n_propagation = 3,
  alpha = 10,
  filter_links = TRUE,
  overwrite = FALSE,
  n_pca_dims = 50,
  k = NULL,
  bagging_number = 20,
  n_jobs = -1,
  base_GRN = NULL,
  counts_layer = "counts",
  embedding = "umap",
  features = NULL,
  ...
)
}
\arguments{
\item{net_struc}{A \code{bn.fit} network structure.}

\item{data}{A data frame or Seurat object containing expression data.}

\item{meta}{A character or factor vector of cell-type labels corresponding to
the columns of \code{data} in single-cell mode.}

\item{index}{A list of column indices, one element per cell sample.}

\item{n_sample}{Number of cell samples.}

\item{n_permutation}{Number of random permutations per cell sample when
\code{perturb_ratio = "permutation"}. Ratio perturbation and deletion each
produce one perturbation result per cell sample.}

\item{perturb_ratio}{A non-negative numeric scalar by which expression of
the perturbed feature is multiplied, \code{"deletion"} to remove each feature
in turn, or \code{"permutation"} to randomly permute each feature across cells.
A value of \code{0} performs zeroing, values between \code{0} and \code{1}
perform knockdown, and values greater than \code{1} perform knockup.}

\item{mode}{Character string specifying the input data mode:
\code{"single_cell"} or \code{"bulk"}.}

\item{perturbation}{Character string specifying the perturbation mode:
\code{"single_feature"} for GRN-free perturbation or \code{"GRN"} for
CellOracle-based GRN perturbation.}

\item{ncores}{Number of cores used for parallel computation.}

\item{verbal}{Logical; whether to print progress messages.}

\item{replace}{Logical; whether permutation sampling is performed with
replacement. It is not used by ratio perturbation or deletion.}

\item{oracle}{A CellOracle Oracle object or file path used in GRN mode.}

\item{links}{A CellOracle Links object or file path used in GRN mode.}

\item{save_dir}{Directory for CellOracle and intermediate SCM results.}

\item{group.by}{Cell-type column in the Seurat metadata.}

\item{assay}{Assay used for SCM calculation in GRN mode.}

\item{layer}{Expression layer used for SCM calculation in GRN mode.}

\item{genes}{Genes to perturb in GRN mode; \code{NULL} uses all available
regulatory genes.}

\item{perturb_value}{Target expression value passed to CellOracle in GRN
mode.}

\item{n_propagation}{Number of CellOracle propagation steps.}

\item{alpha}{Ridge coefficient used by CellOracle.}

\item{filter_links}{Logical; whether to filter the CellOracle Links object.}

\item{overwrite}{Logical; whether to overwrite cached SCM results.}

\item{n_pca_dims}{Number of principal components used for KNN imputation.}

\item{k}{Number of nearest neighbours.}

\item{bagging_number}{Number of bagging estimators.}

\item{n_jobs}{Number of CPU cores used by CellOracle.}

\item{base_GRN}{Base GRN used when \code{oracle} or \code{links} is \code{NULL}.}

\item{counts_layer}{Raw-count layer used to build the CellOracle object.}

\item{embedding}{Seurat dimensional reduction used by CellOracle.}

\item{features}{Genes used when building the CellOracle object.}

\item{...}{Additional arguments passed to \code{GRNPerturbResult()} in GRN mode.}
}
\value{
A list containing reference and perturbed graph coefficients.
}
\description{
\code{PerturbResult()} calculates graph coefficients from reference and perturbed
expression data. By default, each gene is perturbed in turn by multiplying
its expression by \code{perturb_ratio}. Use \code{perturb_ratio = "deletion"} for
feature deletion or \code{perturb_ratio = "permutation"} for random permutation
across cells.
}
